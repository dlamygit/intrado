---

- name: Subscribe CUCM 
  command: '/usr/bin/python axl-client.py {{ host_app_username|quote }} {{ host_app_password|quote }} CUCM CUCM {{ clone_item.answer_file_config.host_name|quote }} {{ cucm_primary_host_ip|quote }}'
  register: cucm_subscription
  delegate_to: localhost