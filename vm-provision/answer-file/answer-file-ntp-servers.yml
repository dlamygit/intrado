---

- name: Add child element
  xml:
    path: "{{ item.file_path }}"
    xpath: /PlatformData
    add_children:
      - NtpServer: ''

- name: Add child element
  xml:
    path: "{{ item.file_path }}"
    xpath: /PlatformData/NtpServer
    add_children:
      - ParamNameText: "{{ item.param_name_text }}"
      - ParamDefaultValue: "{{ item.param_default_value }}"
      - ParamValue: "{{ item.param_value }}"